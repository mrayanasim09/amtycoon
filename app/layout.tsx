import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { AuthProvider } from "@/lib/auth-context"
import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/toaster"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "AM Tycoons - Find Your Perfect Drive",
  description:
    "Premium used cars dealership. Browse our collection of quality pre-owned vehicles with detailed inspections and warranties.",
  keywords: "used cars, car dealership, pre-owned vehicles, quality cars, car sales",
  openGraph: {
    title: "AM Tycoons - Find Your Perfect Drive",
    description: "Premium used cars dealership with quality pre-owned vehicles",
    type: "website",
  },
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          <AuthProvider>
            {children}
            <Toaster />
          </AuthProvider>
        </ThemeProvider>
      </body>
    </html>
  )
}
